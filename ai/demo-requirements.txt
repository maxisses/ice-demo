# Deliberately behind, so the security demo has something to find.
#
# The real requirements.txt is patched - pip-audit reports it clean. That makes
# for an honest codebase and a dull demo, so this file keeps the pins from
# before the fix. Point the scan at it:
#
#   python3.11 -m pip_audit -r ai/demo-requirements.txt
#   python3.11 ai/dependency-review.py ai/demo-requirements.txt
#
# Never install from this file.
Flask==3.1.0
requests==2.31.0
urllib3==2.0.4
Jinja2==3.1.2
